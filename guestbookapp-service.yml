apiVersion: v1
kind: Service
metadata:
  name: guestbookapp-service
  labels:
   app: guestbook-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    name: guestbookapp
    app: guestbook-app
